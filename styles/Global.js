import { StyleSheet } from "react-native";

export const GlobalStyles = StyleSheet.create({
    container: {
        flex: 1,
        padding: 30,
    },  
    headerText: {
        fontFamily: "LatoBold",
        fontSize: 40,
    }
});